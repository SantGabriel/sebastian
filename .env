# Caminho fixo e idêntico para todo mundo que clona o projeto — não é segredo,
# é só a localização do banco SQLite local. Resolvido relativo a prisma/schema.prisma.
DATABASE_URL="file:../db/sebastian.db"
